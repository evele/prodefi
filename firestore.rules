rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Waitlist writes go through the Admin SDK in Cloud Functions.
    match /waitlist/{document=**} {
      allow read, write: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
